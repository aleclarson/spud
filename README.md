# spud

```js
struct User {
  name: string
  age?: number
}

// Class instances
let user = User {
  name: 'Spud'
}

// Object literals
let options = {
  name: 'Spud'
  age: none // The "none" literal
}
```
